# NSFW Engine (Not So Fast Working Engine)

Учебный проект 2D игрового движка на C++ с использованием Raylib и STL

## Описание

NSFW Engine - это образовательный проект, демонстрирующий разработку простого 2D игрового движка с нуля. Проект реализует современные архитектурные паттерны и подходы, используемые в gamedev индустрии.

## Основные возможности

### Архитектура ECS (Entity-Component-System)
- Гибкая система управления сущностями
- Компонентная архитектура для переиспользования функционала
- Системы для обработки логики (камера, рендеринг, масштабирование)

### Движок рендеринга
- Система рендеринга на базе Raylib
- Система камеры с поддержкой панорамирования и масштабирования
- Менеджер текстур для эффективного управления ресурсами

### Система событий
- Event-driven архитектура
- Обработка событий ввода и окна
- EventTranslator для преобразования событий Raylib

### Утилиты
- Математические классы (Vector2D, Rectangle)
- QuadTree для пространственного разбиения
- Вспомогательные инструменты для разработки

## Начало работы

### Требования

- **Компилятор**: С++ 23
- **CMake**: версия 3.11 или выше
- **Raylib**: 5.5 (загружается автоматически через CMake)
- **Google Test**: 1.14.0 (для тестов, загружается автоматически)

### Сборка проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/SissyBoy1818/NotSoFastWorking_engine
cd NSFW_engine

# Создайте директорию для сборки
mkdir build
cd build

# Генерация проекта
cmake ..

# Сборка
cmake --build .

# Запуск
./NotSoFastWorking_engine
```

### Для Windows (PowerShell)

```powershell
# Создайте директорию для сборки
mkdir build; cd build

# Генерация и сборка
cmake ..; cmake --build .

# Запуск
.\Debug\NotSoFastWorking_engine.exe
```

## 📚 Использование

Пример создания простого уровня с камерой:

```cpp
#include "Engine.h"
#include "CameraSystem.h"
#include "DrawableSystem.h"

int main() {
    // Создание движка
    nsfw::core::EngineConfig config{800, 450, "My Game"};
    nsfw::core::Engine game(config);
    
    // Создание уровня
    auto level = std::make_shared<nsfw::core::Level>();
    
    // Создание камеры
    auto camera = level->getEntityManager().create();
    nsfw::ecs::Transform transform;
    transform.rectangle.position = {0.0f, 0.0f};
    transform.rectangle.size = {800.0f, 450.0f};
    level->addComponent(camera, transform);
    
    nsfw::ecs::CameraComponent cameraComp;
    level->addComponent(camera, cameraComp);
    
    // Добавление уровня в движок
    game.addLevel(level);
    
    // Запуск игрового цикла
    game.run();
    
    return 0;
}
```

## Реализованные функции

- Базовый игровой цикл с подсчетом deltaTime
- ECS архитектура с менеджерами компонентов и систем
- Система уровней (Level)
- Система камеры с панорамированием и зумом
- Рендеринг примитивов и текстур
- Обработка событий ввода и окна
- Менеджер текстур
- Модульное тестирование с Google Test

## Планируемые функции

- Физический движок
- Система анимации
- Аудио система
- UI система
- Профилирование и отладка

## Тестирование

Проект использует Google Test для модульного тестирования:

```bash
# Запуск тестов
.\Debug\tests.exe
```

## 🛠Технологии

- **C++ 23**: Современные возможности языка
- **Raylib 5.5**: Библиотека для работы с графикой и вводом
- **STL**: Стандартная библиотека шаблонов
- **Google Test**: Фреймворк для тестирования
- **CMake**: Система сборки

---

*Проект находится в активной разработке*

